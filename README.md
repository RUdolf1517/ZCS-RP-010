# ZCS-RP-010: Система управления достижениями учеников

Система для учета достижений учеников в образовательных учреждениях.

## 🚀 Быстрый старт

### 1. Клонирование и установка

```bash
git clone <repository-url>
cd project
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

#### Вариант 1: Автоматическая настройка (рекомендуется)

```bash
python setup_env.py
```

Скрипт автоматически:
- Сгенерирует безопасный секретный ключ
- Запросит настройки сервера
- Создаст учетную запись администратора
- Сохранит все в `.env` файл

#### Вариант 2: Ручная настройка

Скопируйте файл с примером настроек:

```bash
cp env_example.txt .env
```

Отредактируйте `.env` файл и установите свои значения:

```env
# ОБЯЗАТЕЛЬНО измените эти значения для безопасности!
FLASK_SECRET_KEY=your-very-secure-secret-key-here
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your-secure-admin-password
```

### 3. Запуск приложения

```bash
python app.py
```

Приложение будет доступно по адресу: http://localhost:5001

## 🔧 Переменные окружения

| Переменная | Описание | Значение по умолчанию |
|------------|----------|----------------------|
| `FLASK_HOST` | Хост сервера | `0.0.0.0` |
| `FLASK_PORT` | Порт сервера | `5001` |
| `FLASK_DEBUG` | Режим отладки | `True` |
| `FLASK_SECRET_KEY` | Секретный ключ сессий | ⚠️ ОБЯЗАТЕЛЬНО изменить |
| `ADMIN_USERNAME` | Логин администратора | `admin` |
| `ADMIN_PASSWORD` | Пароль администратора | ⚠️ ОБЯЗАТЕЛЬНО установить |

## 🔐 Безопасность

⚠️ **ВАЖНО:** Перед запуском в продакшене обязательно:

1. Измените `FLASK_SECRET_KEY` на случайную строку
2. Установите надежный `ADMIN_PASSWORD`
3. Установите `FLASK_DEBUG=False`
4. Настройте HTTPS

## 🎯 Возможности

### Для администраторов:
- Управление параллелями классов
- Создание и редактирование классов
- Управление учениками
- Экспорт данных в Excel
- Управление пользователями

### Для классных руководителей:
- Управление своим классом
- Добавление учеников
- Скачивание отчетов класса

### Для учителей:
- Добавление достижений ученикам

## 📊 Структура БД

- **Grades** - Параллели классов (7-11 классы)
- **SchoolClasses** - Классы (7А, 8Б и т.д.)
- **Students** - Ученики с достижениями
- **AdminUsers** - Пользователи системы

## 🛠️ Разработка

### Запуск тестов

```bash
python test_new_structure.py
```

### Структура проекта

```
project/
├── app.py              # Основное приложение Flask
├── database.py         # Модели и функции БД
├── templates/          # HTML шаблоны
├── static/             # Статические файлы (CSS, JS)
├── .env                # Переменные окружения (не коммитить!)
├── env_example.txt     # Шаблон переменных окружения
├── requirements.txt    # Зависимости Python
└── README.md           # Этот файл
```

## 📝 API

### Админ-панель
- `GET /admin` - Главная панель администратора
- `GET /admin/login` - Вход в систему
- `POST /admin/logout` - Выход из системы

### Управление классами
- `GET /admin/classes` - Список классов
- `GET /admin/classes/new` - Создание класса
- `POST /admin/classes/new` - Сохранение нового класса
- `GET /admin/classes/{id}/edit` - Редактирование класса
- `POST /admin/classes/{id}/edit` - Сохранение изменений
- `POST /admin/classes/{id}/delete` - Удаление класса

### Управление учениками
- `GET /admin/students/new` - Создание ученика
- `POST /admin/students/new` - Сохранение ученика
- `GET /admin/students/{id}/edit` - Редактирование ученика
- `POST /admin/students/{id}/edit` - Сохранение изменений
- `POST /admin/students/{id}/delete` - Удаление ученика

### Экспорт данных
- `GET /admin/export/excel` - Экспорт всех учеников
- `GET /admin/export/excel_by_class` - Экспорт по классам
- `GET /admin/classes/{id}/export` - Экспорт конкретного класса

## 📄 Лицензия

CC BY-NC-SA License

## 👥 Авторы

Rudolf1517