# Панель управления достижениями учеников ZCS-RP-010

Веб-приложение для учета и управления достижениями учеников в конкурсах и олимпиадах.

> 🇬🇧 **English documentation**: [README.md](README.md)

## Возможности

- 📊 **Управление карточками учеников** - Создание, редактирование и управление профилями учеников
- 🏆 **Детальный учет достижений** - Запись достижений с указанием:
  - Название конкурса/олимпиады
  - Уровень (Школьный/Районный/Региональный/Всероссийский/Международный)
  - Результат (Участник/Призёр/Победитель)
  - Учебный год (формат: 25/26, 26/27 и т.д.)
  - Дата участия
- 📈 **Экспорт в Excel** - Экспорт данных в различных форматах:
  - Экспорт всех учеников
  - Экспорт по классам (выбор из выпадающего списка)
  - Все достижения экспортируются с полными деталями
- 🔍 **Поиск и фильтрация** - Поиск по имени, фильтрация по классу
- 👥 **Публичный просмотр** - Публичная страница для просмотра достижений учеников
- 🔐 **Админ-панель** - Безопасный интерфейс администратора для управления
- 🚀 **Готово к продакшену** - Поддержка systemd service для работы 24/7

## Требования

- Python 3.8 или выше
- pip (менеджер пакетов Python)
- Ubuntu Server 18.04+ / Debian 10+ (для автоматической установки)

## Установка

### Ubuntu Server / Debian

1. Клонируйте или скачайте проект на ваш сервер
2. Перейдите в директорию проекта
3. Запустите установщик:

```bash
chmod +x install.sh
sudo ./install.sh
```

Установщик выполнит:
- Проверку и установку системных зависимостей
- Создание виртуального окружения
- Установку Python пакетов
- Запрос пароля администратора и настройки конфигурации
- Инициализацию базы данных
- Опциональную настройку systemd service для автозапуска

**Во время установки вам будет предложено:**
- Имя пользователя администратора (по умолчанию: `admin`)
- Пароль администратора (с подтверждением)
- Порт сервера (по умолчанию: `5000`)
- Хост сервера (по умолчанию: `0.0.0.0`)
- Настроить ли systemd service для автозапуска

### Ручная установка

Если установщик не работает, установите вручную:

```bash
# Создание виртуального окружения
python3 -m venv .venv

# Активация виртуального окружения
source .venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt

# Инициализация базы данных
python3 -c "from database import init_db; init_db()"

# Создание .env файла (скопируйте из .env.example и заполните значения)
```

## Конфигурация

Конфигурация хранится в файле `.env` (создается автоматически при установке):

```env
ADMIN_USERNAME=admin
ADMIN_PASSWORD=ваш_безопасный_пароль
FLASK_SECRET_KEY=ваш_секретный_ключ
FLASK_HOST=0.0.0.0
FLASK_PORT=5000
FLASK_DEBUG=False
```

## Запуск приложения

### Использование скрипта запуска

```bash
./run.sh
```

### Ручной запуск

```bash
source .venv/bin/activate
python3 app.py
```

### Использование systemd service (Продакшен)

Если systemd service был настроен во время установки:

```bash
# Запустить сервис
sudo systemctl start zcs-rp-010

# Остановить сервис
sudo systemctl stop zcs-rp-010

# Проверить статус
sudo systemctl status zcs-rp-010

# Просмотр логов
sudo journalctl -u zcs-rp-010 -f

# Включить автозапуск при загрузке
sudo systemctl enable zcs-rp-010
```

Приложение будет доступно по адресу: **http://ip-вашего-сервера:5000**

## Доступ

### Админ-панель

- **URL**: http://ip-вашего-сервера:5000/admin/login
- **Имя пользователя**: Установлено при установке (по умолчанию: `admin`)
- **Пароль**: Установлен при установке

> ⚠️ **Важно**: Измените пароль администратора по умолчанию в продакшене!

### Публичный просмотр

- **URL**: http://ip-вашего-сервера:5000

## Использование

### Создание карточки ученика

1. Войдите в админ-панель
2. Нажмите "Новая карточка"
3. Заполните обязательные поля:
   - ФИО
   - Класс
   - Кл. руководитель
4. Добавьте достижения:
   - Нажмите "Добавить достижение"
   - Заполните: Название конкурса, Уровень, Результат, Учебный год, Дата
   - Добавьте столько достижений, сколько нужно
5. Нажмите "Сохранить"

### Экспорт данных

В админ-панели вы можете экспортировать данные тремя способами:

1. **Экспорт всех** - Экспортирует всех учеников в Excel
2. **Экспорт класса** - Выберите класс из выпадающего списка для экспорта
3. Каждое достижение экспортируется как отдельная строка с полными деталями

Экспортированные Excel файлы включают:
- ID ученика, ФИО, Класс, Кл. руководитель
- Название достижения, Уровень, Результат, Год, Дата
- Дата создания

## Структура проекта

```
project/
├── app.py                 # Основное Flask приложение
├── database.py            # Модели базы данных и функции
├── config.py              # Модуль конфигурации
├── requirements.txt       # Python зависимости
├── install.sh             # Скрипт установки (Ubuntu/Debian)
├── run.sh                 # Скрипт запуска
├── .env                   # Переменные окружения (создается при установке)
├── app.db                 # SQLite база данных (создается автоматически)
├── templates/             # HTML шаблоны
│   ├── base.html
│   ├── index.html
│   ├── admin_dashboard.html
│   ├── admin_student_form.html
│   └── admin_login.html
└── static/                # Статические файлы (CSS)
    └── style.css
```

## Развертывание в продакшене

Для развертывания в продакшене рекомендуется:

1. **Использовать systemd service** (уже включено в установщик)
2. **Настроить Nginx как reverse proxy**:
   ```nginx
   server {
       listen 80;
       server_name ваш-домен.com;
       
       location / {
           proxy_pass http://127.0.0.1:5000;
           proxy_set_header Host $host;
           proxy_set_header X-Real-IP $remote_addr;
       }
   }
   ```
3. **Настроить SSL сертификат** (Let's Encrypt):
   ```bash
   sudo apt-get install certbot python3-certbot-nginx
   sudo certbot --nginx -d ваш-домен.com
   ```
4. **Настроить файрвол**:
   ```bash
   sudo ufw allow 80/tcp
   sudo ufw allow 443/tcp
   sudo ufw enable
   ```

## Разработка

### Запуск в режиме отладки

Отредактируйте файл `.env`:
```env
FLASK_DEBUG=True
```

Или установите переменную окружения:
```bash
export FLASK_DEBUG=True
python3 app.py
```

## Решение проблем

### Сервис не запускается

Проверьте логи:
```bash
sudo journalctl -u zcs-rp-010 -n 50
```

### Ошибки базы данных

Переинициализируйте базу данных:
```bash
python3 -c "from database import init_db; init_db()"
```

### Ошибки прав доступа

Убедитесь в правильных правах:
```bash
chmod +x install.sh run.sh
chmod 600 .env
```

## Особенности

### Уровни конкурсов/олимпиад

- **Школьный** - Конкурсы на уровне школы
- **Районный** - Конкурсы на уровне района
- **Региональный** - Конкурсы на уровне региона
- **Всероссийский** - Всероссийские конкурсы
- **Международный** - Международные конкурсы

### Результаты участия

- **Участник** - Участие в конкурсе
- **Призёр** - Занял призовое место
- **Победитель** - Занял первое место

### Формат учебного года

Учебный год указывается в формате `25/26`, где:
- `25` - последние две цифры года начала учебного года (2025)
- `26` - последние две цифры года окончания учебного года (2026)

## Безопасность

- Пароли администратора хранятся в `.env` файле (не коммитятся в git)
- Секретный ключ Flask генерируется автоматически при установке
- Рекомендуется использовать HTTPS в продакшене
- Файл `.env` имеет права доступа 600 (только владелец может читать)

## Лицензия

This work is licensed under a [Creative Commons Attribution-NonCommercial-ShareAlike 4.0 International License][cc-by-nc-sa].

[cc-by-nc-sa]: http://creativecommons.org/licenses/by-nc-sa/4.0/
[cc-by-nc-sa-image]: https://licensebuttons.net/l/by-nc-sa/4.0/88x31.png
[cc-by-nc-sa-shield]: https://img.shields.io/badge/License-CC%20BY--NC--SA%204.0-lightgrey.svg

## Поддержка

По вопросам и проблемам обращайтесь к документации проекта или к разработчику.

---

**ZCS-RP-010** - Панель управления достижениями учеников

